# Trained weights

Place the validated checkpoint here as:

`best_integrated_model.pth`

The checkpoint must contain a PyTorch `model_state_dict` compatible with `models/integrated_model.py`.

The validated development run used the best checkpoint from epoch 16 and reported a mean validation PSNR of 27.947178 dB.

For the final submission, upload the actual weight file or provide a direct downloadable release/LFS link. Do not replace it with a placeholder file.
